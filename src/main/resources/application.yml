# 应用服务 WEB 访问端口（修复语法：冒号后加空格）
server:
  port: 9093  # 关键：这里必须有空格！

# 数据库配置（如果后续需要用，取消注释后同样要注意语法）
#spring:
#  datasource:
#    url: jdbc:mysql://localhost:3306/histories?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
#    username: root  # 冒号后有空格，正确
#    password: 123456  # 正确
#    driver-class-name: com.mysql.cj.jdbc.Driver  # 正确
#  jpa:
#    hibernate:
#      ddl-auto: update  # 正确
#    show-sql: true  # 正确
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL5InnoDBDialect  # 正确